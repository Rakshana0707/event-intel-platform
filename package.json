{
  "name": "event-intel-platform",
  "version": "1.0.0",
  "description": "A full-stack React and Express data intelligence dashboard",
  "scripts": {
    "postinstall": "npm install --prefix node-backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "node node-backend/server.js",
    "dev:backend": "npm run dev --prefix node-backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "",
  "license": "ISC"
}
